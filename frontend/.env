# Base URL for the real backend. Ignored while VITE_USE_MOCK=true.
VITE_APP_API_URL=http://localhost:3000/api

# Dev port
VITE_APP_PORT=5173

# true = attach axios-mock-adapter to utils/api, serving in-memory fixtures.
# Set to false once the backend is reachable.
VITE_USE_MOCK=true
